export const gameScoreFormat = new Intl.NumberFormat('en-US', {
    minimumIntegerDigits: 5,
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
});

export const accuracyFormat = new Intl.NumberFormat('en-US', {
    minimumIntegerDigits: 2,
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
});

export const gamesPlayedFormat = new Intl.NumberFormat('en-US', {
    minimumIntegerDigits: 1,
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
});